% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_flowpaths.R
\name{trace_flowpaths}
\alias{trace_flowpaths}
\title{Generate upstream and downstream flowpath lookup tables}
\usage{
trace_flowpaths(
  input,
  temp_dir = NULL,
  pwise_dist = FALSE,
  pwise_all_links = FALSE,
  return_products = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{input}{An \code{ihydro} object from \code{\link[=generate_vectors]{generate_vectors()}}.}

\item{temp_dir}{Temporary file directory.}

\item{pwise_dist}{Logical. Calculate all downstream flow-connected pairwise distances?}

\item{pwise_all_links}{Logical. Calculate all flow un-connected pairwise distances?}

\item{return_products}{Logical. Return GIS products?}

\item{verbose}{Logical.}
}
\value{
An \code{ihydro} object.
The contained tables in the output GeoPackage are:
\itemize{
\item \code{ds_flowpaths}: Downstream flowpaths for each link.
\item \code{us_flowpaths}: Upstream flowpaths for each link.
\item \code{funcon_pwise_dist}: Pairwise flow-unconnected distances between links (if \code{pwise_dist = TRUE}).
\item \code{fcon_pwise_dist}: Pairwise flow-connected distances between links (if \code{pwise_dist = TRUE}).
The function returns an \code{ihydro} object containing references to the flowpath tables, which can be accessed using \code{\link[=read_ihydro]{read_ihydro()}} or listed with \code{\link[=ihydro_layers]{ihydro_layers()}}.
}
}
\description{
Traces upstream and downstream flowpaths from each stream link, storing results
in the GeoPackage database tables \code{ds_flowpaths} and
\code{us_flowpaths}. Optionally computes pairwise distances using \code{\link[=generate_pdist]{generate_pdist()}}.
}
\details{
This function performs a breadth-first traversal of the stream network to identify all flow-connected paths downstream and upstream of each link. It uses the \code{stream_links_attr} table generated by \code{\link[=generate_vectors]{generate_vectors()}} as input, which contains attributes for each stream link including its immediate upstream and downstream connections. The function iteratively builds the downstream paths starting from exit tributaries (links with no downstream connection) and then traces upstream paths by reversing the downstream paths. Results are stored in the GeoPackage database for efficient querying.
If \code{pwise_dist} is \code{TRUE}, the function will also compute pairwise flow-connected distances between links using \code{\link[=generate_pdist]{generate_pdist()}}, which can be used for weighted analyses. Setting \code{pwise_all_links} to \code{TRUE} will compute distances for all pairs of links, regardless of flow connectivity, which can be useful for certain applications but may be computationally intensive for large networks.

The function is designed to be efficient and can handle large networks, but the runtime may increase with the size and complexity of the stream network, especially if \code{pwise_dist} is \code{TRUE}. It is recommended to run this function on a machine with sufficient resources and to use the \code{verbose} option to monitor progress.
}
\examples{
\dontrun{
# Example usage
tdir <- tempdir()

ex_dem <- ex_data("elev_ned_30m.tif")
ex_streams <- ex_data("streams.shp")
ex_points <- ex_data("sites_nc.shp")
output_gpkg <- file.path(tdir, "output.gpkg")

ihydro_obj <- ihydro::process_flowdir(
   dem = ex_dem,
   threshold = 1000,
   burn_streams = ex_streams,
   burn_depth = 5,
   burn_slope_dist = 250,
   burn_slope_depth = 5,
   min_length = 3,
   depression_corr = "breach",
   output_filename = output_gpkg,
   return_products = TRUE,
   verbose = TRUE
)

ihydro::generate_vectors(
   input = ihydro_obj,
   points = ex_points,
   site_id_col = "site_id",
   snap_distance = 150,
   break_on_noSnap = FALSE,
   return_products = TRUE,
   verbose = TRUE
)

ihydro::trace_flowpaths(
  input = ihydro_obj,
  pwise_dist = TRUE,
  pwise_all_links = TRUE,
  return_products = TRUE,
  verbose = TRUE
)

ihydro::ihydro_layers(ihydro_obj, n = Inf)

# get all upstream link_ids from link_id 800
us_800 <- filter(ihydro::read_ihydro(ihydro_obj, "us_flowpaths"), pour_point_id == "800")

# get all downstream link_ids from link_id 800
ds_800 <- filter(ihydro::read_ihydro(ihydro_obj, "ds_flowpaths"), pour_point_id == "800")

lines_out <- ihydro::read_ihydro(ihydro_obj, "stream_lines") |>
 dplyr::filter(
  link_id \%in\% us_800$origin_link_id |
  link_id \%in\% ds_800$destination_link_id
 )

mapview::mapview(lines_out)

}

}
